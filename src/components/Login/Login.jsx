import PropTypes from 'prop-types';
import { Link } from 'react-router-dom';
import './Login.css';

const Login = () => {
  return (
    <div className='Login'>
      Login Placeholder
    </div>
  );
};

Login.propTypes = {
};

export default Login;